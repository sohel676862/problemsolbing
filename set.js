const num=[1,2,5,3,4,5,6,6,7,7,8,12,10]


const removedoplicat=(arr)=>{

    let newarr=[];
    // console.log(newarr)
    for(let i=0;i<arr.length;i++){
        let element=arr[i];


        if(!newarr.includes(element))
    }

    // console.log(newarr)
    return newarr;
}

const result= removedoplicat(num)
// console.log(result)

